void main() {
  // 3. Declare a var variable and assign a string value to it.
  // Then reassign an integer to the same variable. Print the variable after each assignment.

  var name = 'rania';
  print(name + '\n');
  // name is String datatype by using var keyword
  name = 20;
  // i can't assign int value in String datatype
  print(name);
}
